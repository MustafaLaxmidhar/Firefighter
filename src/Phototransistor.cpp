#include "Phototransistor.h"

int voltGrey(){
  analogRead(whatever pin);
}

int voltPurple(){
  analogRead(whatever pin);
}

int voltGreen(){
  analogRead(whatever pin);
}

int voltBlue(){
  analogRead(whatever pin);
}

