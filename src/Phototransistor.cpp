#include "Phototransistor.h"

int voltGrey(){
  analogRead(whatever pin);
}

float distGrey();

int voltPurple(){
  analogRead(whatever pin);
}

float distPurple();

int voltGreen(){
  analogRead(whatever pin);
}

float distGreen();

int voltBlue(){
  analogRead(whatever pin);
}

float distBlue();
